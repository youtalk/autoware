# Incompatible image set: the same 0.1.0 provider with a consumer built against MAJOR 1.
# deploy_check.sh must REJECT this set with a MAJOR mismatch (exit 1).
services:
  provider:
    image: oss-if-gate-self-test-provider
  consumer:
    image: oss-if-gate-self-test-consumer-incompatible
